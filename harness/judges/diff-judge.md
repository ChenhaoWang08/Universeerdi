# Diff Judge

Review the change set with:

- `git status`
- `git diff --stat`
- `git diff --name-only`

Pass only if:

- the diff stays within allowed files
- PR3 changes focus on stabilization, data modeling, tests, and related docs
- unrelated rewrites are absent
- the change remains small enough to review confidently
